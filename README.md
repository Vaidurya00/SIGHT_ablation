# SIGHT_ablation

Ablation and experiments for SIGHT (search-augmented agentic RL), based on the ARPO codebase.

## Structure

- `ARPO/` — Training scripts, rollout code, reward functions, and configs for SIGHT / IGD ablations.

## Usage

See `ARPO/scripts/` for run scripts (e.g. `SIGHT_3b_ours5_0205.sh`). Configure paths and hyperparameters in the script before running.
